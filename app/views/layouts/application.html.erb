<!DOCTYPE html>
<html>
<head>
  <title>Bramble</title>
  <%= stylesheet_link_tag 'bramble' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="comm">
    <% if user_signed_in? %>
      Signed in as <%= current_user.email %>. Not You?
      <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :class => "brutton" %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path, :class => "brutton" %> <%= link_to "Sign In", new_user_session_path, :class => "brutton" %>
    <% end %>
    
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
    <% end %>
  </div>
  <!--<%#TODO: Add button styles, change the formatting for the top and bottom bars -->
  
  <br/>
  
  <div id="page">
    <%= yield %>
  </div>
  
  <br/>
  
  <div id="footer">
    <%= link_to "Home", root_path, :class => "brutton" %>
  </div>
</body>
</html>
